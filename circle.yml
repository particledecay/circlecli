general:
  artifacts:
    - htmlcov

dependencies:
  override:
    - pip install -r requirements_dev.txt

test:
  post:
    - coverage run test_circlecli.py
    - coverage report -m
    - coverage html
