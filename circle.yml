general:
  artifacts:
    - htmlcov

dependencies:
  override:
    - pip install -r requirements_dev.txt

test:
  override:
    - nosetests --cover-branches --with-coverage --cover-erase --cover-package=circlecli --cover-html 
