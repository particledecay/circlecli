general:
  artifacts:
    - htmlcov

dependencies:
  override:
    - pip install -r requirements_dev.txt

test:
  post:
    - coverage run circlecli.py --include ./
    - coverage report
    - coverage html
