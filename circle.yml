general:
  artifacts:
    - htmlcov

dependencies:
  override:
    - pip install -r requirements_dev.txt

test:
  override:
    - nosetests -cover-package=circlecli
  post:
    - coverage report -m
    - coverage html
